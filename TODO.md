# TODO

## Parser
- [x] Зафиксировать формат ввода (например, `<duration> <text>`) и описать его в комментариях.
- [x] Добавить детальные сообщения об ошибках, которые можно напрямую показывать пользователю.
- [x] Покрыть парсер тестами на валидные/невалидные входы, в том числе сложные длительности.

## Store
- [x] Реализовать `NextTask(ctx)` в `MemoryStore` для поиска ближайшей задачи.
- [x] Обеспечить копирование срезов в `ListTasks`, чтобы избежать гонок данных.
- [x] Добавить тесты для `CreateTask`, `DeleteTask`, `NextTask`, включая пустое состояние.
- [x] Подготовить TODO-комментарии/технический долг под будущий `DBStore` (схема таблиц, индексы).

## Scheduler
- [x] Заполнить логику `Start`: цикл `NextTask -> time.Timer -> Notify -> DeleteTask -> Refresh`.
- [x] Реализовать метод `Refresh` (безопасная отправка сигнала) и `Stop` (останавливает таймер и горутину).
- [x] Подключить планировщик в `main.go`, передав в него store, notify-функцию и логгер.
- [x] Написать тесты для планировщика с использованием поддельного хранилища и fake NotifyFunc.

## Notifier
- [x] Определить интерфейс/тип `NotifyFunc`, который будет использовать Telegram API.
- [x] Реализовать функцию-обёртку над `bot.api.Send`, обрабатывающую ошибки и логирование.
- [x] Добавить константы/шаблоны сообщений для напоминаний (например, "Напоминание: <текст>").

## Bot
- [x] Завершить обработку обычных сообщений: парсер -> store -> scheduler.Refresh() -> ответ пользователю.
- [x] Реализовать `/list`: получать задачи из store, форматировать список, отправлять пользователю.
- [x] Реализовать `/delete <id>`: валидация ID, удаление, сигнал планировщику.
- [x] Обработать неожиданные команды/формат входа, отправляя подсказки.
- [x] Вынести help-текст и другие сообщения в отдельный модуль/константы.
- [x] Добавить логирование ключевых событий (добавление, удаление, отправка напоминаний).

## Конфигурация и запуск
- [x] Расширить `config.Config` параметрами таймаутов long polling и уровней логирования.
- [x] Создать сборку/инициализацию в `main.go`: загрузка конфигурации -> создание бота -> запуск планировщика -> start polling.
- [x] Обеспечить корректное завершение: `defer bot.Stop()`, `scheduler.Stop()`, закрытие логгеров.

## Тестирование и качество
- [ ] Настроить `go test ./...` (после получения зависимостей) и описать команду в документации.
- [x] Добавить `go test ./... -race` в чеклист перед релизом.
- [x] Подготовить набор ручных сценариев: добавить напоминание, проверить `/list`, удалить и дождаться уведомления.
